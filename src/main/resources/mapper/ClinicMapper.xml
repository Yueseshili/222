<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.group.weiyi.mapper.ClinicMapper">

    <select id="selectClinicByHospitalAndEpartmene" resultType="com.group.weiyi.entity.Clinic">
        select * from clinic where h_id=#{h_id} and e_id = #{e_id}
    </select>
</mapper>
